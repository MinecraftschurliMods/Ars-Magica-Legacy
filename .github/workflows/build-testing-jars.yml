name: "Build Testing JARs"

on:
  workflow_dispatch:

jobs:
  build:
    uses: MinecraftschurliMods/.github/.github/workflows/build.yml@main
    with:
      java-version: 25
      upload-artifacts: true
      release-type: dev
